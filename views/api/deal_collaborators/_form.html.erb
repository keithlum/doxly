<%= form_for(deal_collaborator) do |f| %>
  <% if deal_collaborator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deal_collaborator.errors.count, "error") %> prohibited this deal_collaborator from being saved:</h2>

      <ul>
      <% deal_collaborator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :deal_id %>
    <%= f.number_field :deal_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :added_by %>
    <%= f.number_field :added_by %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
